# Tells GitHub Pages to serve the files as-is rather than running Jekyll over
# them. Without this, anything in a directory starting with an underscore
# (Astro emits its assets into /_astro/) can be silently dropped.
